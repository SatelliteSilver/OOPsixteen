/* real-time function (Draw) */
void draw() { 
 
  //render part
  renderBackground();
  renderBricks();
  renderBreaker();
  
  //physics engine part 
}

/* real-time function (Key Press) */
void keyPressed() {
  if (key == CODED) {
    if (keyCode == UP) {

    } else if (keyCode == DOWN) {

    } else if (keyCode == 'w') {
    
    } 
  } else {

  }
}
